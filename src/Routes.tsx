//TODO: THis is the React router dom that will show the pages

enum Pages {
    LANDINGPAGE = 0,
    MINTNFT = 1,
    DICEGAME = 2, // Needs to have the ID for the game, dynamic link
    GAMESEARCH = 3 // A page for the indexer that allows users to search games
}

function Router(){
    
}